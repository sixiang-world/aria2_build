name: "Test Dispatch"

on:
  workflow_dispatch:
    inputs:
      test:
        description: "Test input"
        required: false
        default: "hello"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "Test input: ${{ github.event.inputs.test }}"